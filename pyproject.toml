[tool.ruff]
line-length = 88
extend-exclude = [
    ".git",
    "__pycache__",
    ".venv",
    "venv",
    "data",
    "models",
    ".ipynb_checkpoints",
]

[tool.ruff.lint]
extend-select = ["I"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["F821"]
